<%= render "hero" %>

<div class="container container--wide container--no_style">
  <%= render "search", home_variant: true %>
</div>

<div class="container container--wide container--no_style">
  <%= render "list",
    loading: @query.present?,
    frame_src: (@query.present? ? search_google_books_path(query: @query, scope: @search_scope) : nil) %>
</div>
