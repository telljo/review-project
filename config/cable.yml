development:
  adapter: redis
  url: redis://localhost:6379/1

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV["RAILS_ENV"] == "production" ? ENV.fetch("REDIS_URL") : "redis://localhost:6379/1" %>
  channel_prefix: review_editor_production
